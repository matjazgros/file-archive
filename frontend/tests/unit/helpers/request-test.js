import { request } from 'upload-demo/helpers/request';
import { module, test } from 'qunit';

module('Unit | Helper | request');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = request([42]);
  assert.ok(result);
});
